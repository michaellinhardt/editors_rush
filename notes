-> lancer vim sans plugin et specifier fichier config
	vim -u ~/.vimrc --noplugin myfile.c
-> probleme fleche directionnel ABCD (disable vi-compatibility)
	set nocompatible
-> fleche gauche droite change de ligne
	set wichwrap+=<,>,[,]
-> ligne numero et 80eme colone color
	set number
	set cc=80
-> autoindent
	set smartindent
-> auto () et {}
	inoremap -> creer un raccourci clavier
	inoremap( ()<left>
		-> creer le racourci ( qui va ecrire () et <left> decale le curseur
		-> <cr> va a la ligne et <ESC> envoie une touche de raccourci
-> backspace normal
	set backspace=2
	ou
	set backspace=indent,eol,start (indentation, carac precedent, new line)
-> faire ressortir la colone et la ligne actuel du curseur
	set cursorcolumn
	set cursorline
-> hightlight espace en fin de ligne ou sur ligne vide
	match ErrorMsg '\s\+$'
-> les inoremap doivent etre apres les set (a la fin) sinon sa ne marche pas

_____________________ EMACS _____________________________________

-> tab fait des vrai tab
(setq-default c-basic-offset 8
	tab-width 8
	indent-tabs-mode t)
-> auto retour a la ligne avec certains caractere
(add-hook 'c-mode-common-hook '(lambda () (c-toggle-auto-state 1)))
